# Legacy runners

Архив копий `run_pipeline_v*` / `run_v0_*` до введения единого entrypoint (0.7.0).

Не используйте как точку входа. Актуальный запуск:

```bash
.venv/bin/python scripts/run_experiment.py --config configs/experiments/v0.7.0.json
```

Конфиги: `configs/experiments/`. Долг: `reports/TECH_DEBT.md`.

---

历史跑数脚本归档（0.7.0 起统一入口）。请勿作为入口。见上。
